
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" >
    <meta name="viewport" content="width=device-width, initial-scale=1.0" >
    <meta name="theme-color" content="#2196f3" >
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" type="image/png" href="/icons/favicon-96x96.png" sizes="96x96" />
    <link rel="icon" type="image/svg+xml" href="/icons/favicon.svg" />
    <link rel="shortcut icon" href="/icons/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/icons/apple-touch-icon.png" />
    <meta name="apple-mobile-web-app-title" content="SemStore" />

    <title>Sem-Store| IITGN</title>

    <!-- Tailwind CSS CDN -->
    <link href="/src/new.css" rel="stylesheet">

    <!-- Optional: Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">

    <style>
        @keyframes slide-in {
            0% { opacity: 0; transform: translateY(20px); }
            100% { opacity: 1; transform: translateY(0); }
        }
        .animate-slide-in {
            animation: slide-in 0.4s ease-out;
        }
    </style>
</head>

<body class="min-h-screen  flex flex-col text-gray-800">
<!--    changes-->
<%- include('navbar.ejs') %>
<!-- Hero Section -->
<!--<pre id="output"></pre>-->
<main class="flex-grow">
    <header class="text-center py-16 bg-blue-100">
        <h2 class="text-3xl font-bold text-[#1a71c9] mb-4">Empower Learning with Shared Resources</h2>
        <p class="text-lg max-w-xl mx-auto text-gray-700">
            A place for IITGN senior to contribute notes, papers, and more. Juniors can discover curated academic help semester-wise.
        </p>
        <a href="Home"  class="mt-6 inline-block px-6 py-3 bg-blue-600 text-white font-semibold rounded-lg hover:bg-blue-700 transition">
            Start Exploring
        </a>
    </header>

    <!-- Features Section -->
    <section class="max-w-6xl mx-auto px-4 py-12 grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="bg-white p-6 rounded-lg shadow hover:shadow-lg transition">
            <h3 class="text-xl font-semibold text-blue-600">Search & Filter</h3>
            <p class="mt-2 text-gray-700">Quickly find resources by course, semester or type.</p>
        </div>
        <div class="bg-white p-6 rounded-lg shadow hover:shadow-lg transition">
            <h3 class="text-xl font-semibold text-blue-600">Verified Contributions</h3>
            <p class="mt-2 text-gray-700">Seniors upload notes, past papers, and curated materials.</p>
        </div>
        <div class="bg-white p-6 rounded-lg shadow hover:shadow-lg transition">
            <h3 class="text-xl font-semibold text-blue-600">Offline Access</h3>
            <p class="mt-2 text-gray-700">PWA features allow offline view of previously loaded resources.</p>
        </div>
    </section>
    <section class="fixed bottom-4 right-4 z-50 w-full max-w-sm">
        <div id="notificationPanel" class="hidden bg-white border-l-4 border-blue-500 p-4 shadow-lg rounded-lg animate-slide-in">
            <div class="flex justify-between items-start gap-2">
                <div>
                    <p class="font-semibold text-blue-700">New Notification</p>
                    <p class="text-sm text-gray-700 mt-1" id="notificationMessage">
                        Welcome to Sem-Store! Start exploring now.
                    </p>
                </div>
                <button onclick="closeNotification()" class="text-gray-400 hover:text-gray-600">&times;</button>
            </div>
        </div>
    </section>
</main>
<!-- Fixed Notification Panel -->
<aside class="fixed top-24 right-4 w-80 bg-white border border-blue-200 rounded-xl shadow-xl z-50">
    <div class="p-4 flex items-center justify-between border-b">
        <h2 class="text-lg font-semibold text-blue-700">Notifications</h2>
        <button onclick="clearNotifications()" class="text-sm text-blue-500 hover:underline">Clear All</button>
    </div>
    <ul id="notificationList" class="max-h-80 overflow-y-auto divide-y">
        <li class="p-4 text-sm text-gray-700">Welcome to Sem-Store!</li>
        <li class="p-4 text-sm text-gray-700">New notes uploaded in CS101.</li>
        <li class="p-4 text-sm text-gray-700">Your contributor access is approved.</li>
    </ul>
</aside>
<button onclick="togglePanel()" class="fixed bottom-6 right-6 bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-full shadow-xl z-50">
    🔔
</button>
<script>
    function togglePanel() {
        const panel = document.querySelector("aside");
        panel.classList.toggle("hidden");
    }
</script>

<%- include('footer.ejs') %>
<script>
    function showNotification(message) {
        const panel = document.getElementById('notificationPanel');
        const msg = document.getElementById('notificationMessage');
        msg.textContent = message;
        panel.classList.remove('hidden');
        setTimeout(() => {
            panel.classList.add('hidden');
        }, 5000);
    }
    function closeNotification() {
        document.getElementById('notificationPanel').classList.add('hidden');
    }
    // Example trigger
    window.addEventListener('load', () => {
        showNotification("You're viewing the latest resources!");
    });

    function addNotification(msg) {
        const list = document.getElementById('notificationList');
        const item = document.createElement('li');
        item.className = 'p-4 text-sm text-gray-700';
        item.textContent = msg;
        list.prepend(item); // add new at top
    }

    function clearNotifications() {
        document.getElementById('notificationList').innerHTML = '';
    }

    // Example: Show one after page load
    window.addEventListener('load', () => {
        setTimeout(() => {
            addNotification("System update tonight at 11 PM.");
        },1000);
    });
</script>
</body>

</html>